<p id="notice"><%= notice %></p>

<h1>Users</h1>

<%= will_paginate %>

<div>
  <div>
      <span>Name</span>
      <span>Email</span>
  </div>

  <div class="users">
    <%= render @users %>
  </div>
</div>

<%= will_paginate %>

<%= link_to 'New User', new_user_path %>
